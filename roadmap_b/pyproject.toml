[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = ["pretrained: downloads and validates the official pretrained WavLM checkpoint"]

[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
